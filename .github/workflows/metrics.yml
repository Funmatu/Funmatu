name: Metrics
on:
  schedule:
    - cron: "10 0 * * *" 
  workflow_dispatch:
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📊 GitHub Metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          
          # ─── 基本設定 ───
          user: Funmatu
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Asia/Tokyo
          filename: github-metrics.svg          
          debug: no
          plugins_errors_fatal: no

          # ─── 1. 言語統計 (Languages) ───
          plugin_lines: yes
          plugin_languages: yes
          plugin_languages_details: lines, bytes-size, percentage
          plugin_languages_limit: 8

          # ─── 2. 開発習慣 (Habits) ───
          plugin_habits: yes
          plugin_habits_from: 200       # デフォルト推奨値
          plugin_habits_days: 14        # 過去14日間
          plugin_habits_facts: yes      # 事実データの表示
          plugin_habits_charts: yes     # グラフの表示
          plugin_habits_trim: yes       # グラフの平滑化

          # ─── 3. Issue / PR 追跡 (Followup) ───
          plugin_followup: yes
          plugin_followup_sections: repositories

          # ─── 4. GitHub Projects ───
          # 【注意】Projectsを使っていない、またはトークン権限がないとエラーになります。
          # 使用していない場合は 'no' にしてください。
          plugin_projects: no
          plugin_projects_limit: 4

          # ─── 5. 実績 (Achievements) ───
          plugin_achievements: yes
          plugin_achievements_display: compact
          plugin_achievements_threshold: B
          plugin_achievements_ignored: member, maintainer
          plugin_achievements_secrets: yes
